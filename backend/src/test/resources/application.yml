spring:
  datasource:
    url: jdbc:h2:mem:auto_skola_365_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  flyway:
    enabled: false

app:
  security:
    jwt:
      secret: test-only-change-this-secret-test-only-change-this-secret
      expiration-minutes: 60
